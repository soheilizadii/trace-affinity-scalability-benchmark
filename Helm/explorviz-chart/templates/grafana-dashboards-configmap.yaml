apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboards
  labels:
    app: explorviz
    run: grafana
data:
{{ (.Files.Glob "files/monitoring/grafana/src/**").AsConfig | indent 2 }}
