apiVersion: v1
kind: Service
metadata:
  name: user-service
  labels:
    app: explorviz
    run: user-service
spec:
  type: {{ .Values.service.type }}
  selector:
    app: explorviz
    run: user-service
  ports:
    - name: http
      port: {{ .Values.user.port }}
      targetPort: http
      protocol: TCP
