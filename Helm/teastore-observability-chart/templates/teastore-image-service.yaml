apiVersion: v1
kind: Service
metadata:
  name: teastore-image
  labels:
    app: teastore
    run: teastore-image
spec:
  ports:
    - port: {{ .Values.service.port }}
      protocol: TCP
  selector:
    app: teastore
    run: teastore-image
